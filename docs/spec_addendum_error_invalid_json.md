# 仕様書 7.1 エラーコード一覧 追記

以下の 1 行を **7.1 エラーコード一覧** の表に追加してください。  
（`invalid_input` の直前または「型・範囲不正」の説明の前が分かりやすいです。）

| code | HTTP | 説明 |
|------|------|------|
| **invalid_json** | **400** | **JSON 形式不正（Request Body のパース失敗、必須キー欠落など）** |

---

## 追記後の 7.1 一覧イメージ

| code | HTTP | 説明 |
|------|------|------|
| invalid_json | 400 | JSON 形式不正（Request Body のパース失敗、必須キー欠落など） |
| invalid_input | 400 | 型・範囲不正（card が 1〜13 以外、小数、文字列など） |
| invalid_game_state | 400 | 状態不正（…） |
| … | … | … |

※ 既存の行はそのまま残し、上記 1 行だけ追加してください。
